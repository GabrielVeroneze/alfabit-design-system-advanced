import { Canvas, Controls, Meta, Primary } from '@storybook/addon-docs/blocks'
import * as DividerStories from './Divider.stories'

<Meta of={DividerStories} />

# Divisor

Este é um componente de divisor de seções.

### Divisor Padrão

<Canvas of={DividerStories.Default} />

### Divisor com Texto

<Canvas of={DividerStories.DivisorWithText} />

## Documentação Interativa:

<Primary />
<Controls />
